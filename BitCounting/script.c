#include<stdio.h>
#include<stdlib.h>

unsigned	*to_binary(unsigned num)
{
	size_t	counter = 1;
	while (n > 1)
		n % 2 == 0 ? NULL : counter++;
	return counter;
}

size_t	countBits(unsigned value)
{

}
int	main(void)
{
	printf()
	return (0);
}
